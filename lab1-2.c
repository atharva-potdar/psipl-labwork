#include <stdio.h>

/* Lab 1: 2025-09-29
 * 2. Write a C program that uses const keyword. Define a constant for Pi
 *    and calculate the area of a circle.
 */

/*
int main(void)
{
    // Defining the PI constant
    const float PI = 3.14159f;

    // Inputting the radius
    int radius;
    printf("Enter radius: ");
    scanf("%d", &radius);

    // Printing the area of a circle using the formula
    printf("Area of circle = %.2f\n", PI * radius * radius);

    return 0;
}
*/
