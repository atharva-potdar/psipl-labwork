#include <stdio.h>

/* Lab 1: 2025-09-29 [HOME]
 * 5. Write a C program to input marks of three subjects and check
 *    whether the student has passed (all marks >= 40) using
 *    logical operators (&&, ||, !).
 */

/*
int main(void)
{
    // Declare the marks integers
    int marks1, marks2, marks3;

    // Input marks
    printf("Enter marks of 3 subjects: ");
    scanf("%d %d %d", &marks1, &marks2, &marks3);

    // Check if all marks are greater than equal to 40
    if (marks1 >= 40 && marks2 >= 40 && marks3 >= 40)
    {
        // If it is, they passed
        printf("Passed\n");
    }
    else
    {
        // Otherwise, fail
        printf("Failed\n");
    }
    return 0;
}
*/
